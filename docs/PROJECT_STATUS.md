# 功能完成状态

更新日期：2026-08-28。

本文件用于区分“后端已实现”“本地模拟可验收”和“仍需外部服务”的功能，避免前后端或部署人员把占位能力当成生产能力。

当前产品面向住宅、老旧小区和出租屋的火灾风险监测演示，业务参与者、完整处置闭环和迭代优先级见 [业务场景与迭代路线](BUSINESS_SCENARIO_AND_ROADMAP.md)。

## 当前演示部署

| 组件 | 当前配置 | 能力边界 |
| --- | --- | --- |
| 前端 | Cloudflare Pages：`https://easterproject.pages.dev` | GitHub `master` 更新后自动构建 |
| 后端公网入口 | Named Tunnel：`https://api.kangroom.eu.cc` | 域名固定，但依赖本机后端、MySQL 和隧道进程持续运行 |
| 手机告警 | 钉钉 Stream 机器人单聊 | 已绑定员工可接收新告警和广播 |

## 已实现并可本地联调

| 模块 | 当前能力 |
| --- | --- |
| 登录与权限 | JWT 登录、当前用户、本人改密、角色授权、登录限流；`/api/auth/workspace` 按数据库角色返回专属模块和权限；密码修改/重置/账号禁用后旧 JWT 立即失效 |
| 用户管理 | 用户分页、详情、新增、修改、启停、重置密码、删除；保护当前账号和最后一个启用的系统管理员 |
| 用户管理入口 | 后端独立页面 `/admin/index.html`；主前端仅向系统管理员展示控制台入口 |
| 设备管理 | 分页筛选、详情、绑定、编辑、阈值、软解绑、设备令牌创建与轮换 |
| 数据接入 | HTTP 心跳与遥测、华为云 IoTDA MQTT 入站、消息幂等；支持烟雾浓度、环境温湿度、电流、线缆温度、CO 值和蜂鸣器状态；数值保留两位小数 |
| 实时趋势 | 默认展示最近 120 条原始点，每 3 秒刷新；支持 24 小时、7 天和 30 天聚合视图 |
| 角色专属界面 | 居民、小区管理员、系统管理员和消防员具有不同导航、工作台文案和操作入口；居民不读取通知/广播审计，后端继续强制鉴权 |
| 模拟 3D 地图 | 数据库保存楼栋、楼层、房间和设备局部坐标；所有角色可看实时状态，社区/系统管理员可编辑位置；现有设备自动分配模拟位置 |
| 系统能力 | 按运行 Profile 返回开发/生产模式，并实时探测 RAG 知识库连接状态和当前模型信息 |
| 告警处置 | 烟雾、温度、湿度、电流、线缆温度、CO 和离线告警的查询、确认、解决、误报标记及规则辅助复核；支持预警升级危险 |
| 通知查询 | APP、SMS、钉钉通知分页筛选、详情和通道/状态统计；钉钉自动告警记录真实投递结果 |
| 广播管理 | 广播指令创建、查询、再次下发和删除；配置钉钉后可通过机器人单聊下发给所有已绑定员工，并记录成功/失败状态 |
| 智能问答 | 本地知识检索后通过 Ollama 调用 `gpt-oss:120b-cloud`；提供结构化安全处置卡片、领域限制和分层回退，详见 [优化记录](SMART_QA_OPTIMIZATION.md) |
| 接口文档 | 静态 API 文档、前端契约文档和开发环境 Swagger |
| 数据库升级 | 新 schema 使用 `DECIMAL(12,2)` 存储数值遥测并包含 3D 地图表；已有数据库提供浓度、扩展传感器和地图位置升级脚本，后端启动也会兼容补表 |

## 模拟实现或占位能力

| 模块 | 当前行为 | 生产接入要求 |
| --- | --- | --- |
| APP 通知 | 告警生成后写入本地通知中心并标记 `SENT` | 接入真实移动推送 SDK 后由适配器投递 |
| SMS 通知 | 告警生成待发送记录，状态为 `PENDING`，不会实际发送短信 | 接入短信供应商、模板、签名、回执和失败重试 |
| MQTT 广播 | MQTT 入站遥测已实现；广播仍只保存指令和状态，不保证已发布到真实设备 | 定义下行主题并实现发布、设备回执、重试和状态同步 |
| 告警视觉复核 | 基于告警类型、浓度和阈值生成规则结论 | 接入摄像头、图像存储和模型推理服务 |

## 尚未完成的上线条件

- 正式短信/推送供应商账号、模板审核和回执处理。
- MQTT 生产凭据轮换、ACL、设备证书/密钥生命周期、下行发布和高可用 Broker。
- 后端和数据库尚未迁移到长期运行环境；仍需进程守护、WAF、监控告警和备份恢复演练。
- 多实例场景下基于 Redis 或网关的共享登录限流。
- WebSocket/SSE 实时推送；当前前端每 3 秒轮询刷新。
- 面向生产的自动数据库迁移工具；当前使用初始 schema 与兼容性字段补充。

## 前端协作边界

- 主前端可直接对接系统、设备、3D 地图、告警、通知、广播和问答接口。
- 主前端按 `/api/auth/workspace` 返回值生成角色专属界面，并只在开发模式向管理员开放模拟告警；后端权限校验仍是最终边界。
- 用户管理如需集成到新前端，仅 `SYSTEM_ADMIN` 可访问相关 API；已有 `/admin/index.html` 页面可用于临时管理。
- 前端应把 `401` 统一视为会话失效，把 `403` 展示为权限不足，并使用后端 `message` 显示业务错误。
- 前端不得声称 SMS、MQTT 设备广播或视觉 AI 已成功，除非对应外部适配器已经接入并返回成功状态；钉钉单聊广播以钉钉接口返回结果更新状态。

后续功能按 P0 稳定演示、P1 小区试点、P2 生产化和 P3 产品化推进，阶段目标及验收标准见 [迭代路线](BUSINESS_SCENARIO_AND_ROADMAP.md#6-后续迭代方案)。
